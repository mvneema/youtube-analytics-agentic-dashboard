# Trigger

This skill is packaged in a top-level folder named `fable-project` and its `SKILL.md` metadata uses `name: fable-project`.

Invoke it with:

```text
/fable-project
```

You can also pass a task directly:

```text
/fable-project Build a dashboard for my onboarding metrics and discover unknowns first
```
